[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements.txt"]

[phases.build]
cmds = ["python src/ml/train.py || echo 'Training skipped'"]

[start]
cmd = "uvicorn src.api.main:app --host 0.0.0.0 --port $PORT"
